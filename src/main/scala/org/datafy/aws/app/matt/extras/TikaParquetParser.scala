package org.datafy.aws.app.matt.extras

import org.apache.tika.parser.AbstractParser

object TikaParquetParser {
  implicit class TikaParquetParer(parser: AbstractParser) {
    // make some stuff here
  }
}
